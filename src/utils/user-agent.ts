import { version } from "../../package.json";

/**
 * Unique User-Agent string for API Testing App.
 * Format: APIBuddy/<version> (Next.js; Node.js)
 */
export const APP_USER_AGENT = `APIGo/${version}/GracerAPI`;
